$("#modalWindow").html("<%= escape_javascript(render 'financor/taxcodes/home') %>");
$("#modalWindow").modal();
jQuery(document).ready(function() {       
  App.initAjax();
  //FormComponents.init();
});